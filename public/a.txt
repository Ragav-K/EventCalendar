    const alreadyExists = events.some(function(item) {

    return item.name === eventName &&
           item.date === eventDate;

    });

    if (alreadyExists){
        alert("This event is already added to the calendar.");
        return;
    }
